const fmt=(n,d=1)=>Number(n).toLocaleString(undefined,{minimumFractionDigits:d,maximumFractionDigits:d});
const classKey=n=>n.toLowerCase().replaceAll(' ','-');
async function init(){const mode=document.body.dataset.mode,payload=await fetch('/data/benchmarks.json').then(r=>r.json());let rows=payload.rows.filter(x=>mode==='dps'?x.role==='dps':x.role==='tank');const metric=mode==='tank-tps'?'tps':'dps',label=mode==='tank-tps'?'TPS':'DPS';rows.sort((a,b)=>b[metric]-a[metric]);const max=Math.max(...rows.map(x=>x[metric]),1),min=Math.min(...rows.map(x=>x[metric])),total=rows.reduce((a,x)=>a+x[metric],0),mean=total/rows.length;const floor=Math.max(0,min-(max-min)*0.15),span=Math.max(1,max-floor),width=v=>100*(v-floor)/span;document.getElementById('assumptions').textContent=`${payload.duration}s ${payload.encounter} · ${payload.iterations} iterations · full raid defaults · no world buffs`;document.getElementById('summary').innerHTML=`<article><span>Profiles</span><strong>${rows.length}</strong></article><article><span>Highest ${label}</span><strong>${fmt(rows[0][metric])}</strong></article><article><span>Mean ${label}</span><strong>${fmt(mean)}</strong></article><article><span>Fight length</span><strong>${payload.duration}s</strong></article>`;document.querySelector('.benchmark-panel>header span').innerHTML=`Bars span ${fmt(floor,0)}&ndash;${fmt(max,0)} ${label} (range of this list, not zero) &middot; click a row to open its simulator`;document.getElementById('benchmarkRows').innerHTML=rows.map((x,i)=>`<a class="benchmark-row" data-class="${classKey(x.class_name)}" href="${x.url}"><span class="rank">${i+1}</span><img src="/class-icons/${classKey(x.class_name)}.jpg" alt=""><span class="benchmark-name"><strong>${x.class_name} · ${x.name} · ${x.race}</strong><small>${x.role==='tank'?`Tank · ${fmt(x.dps)} DPS · ${fmt(x.tps)} TPS`:`${fmt(x.total_damage,0)} total damage`}</small></span><span class="benchmark-bar" data-tier="${i===0?'top':x[metric]>=mean?'above':'below'}"><i data-bar-width="${width(x[metric]).toFixed(2)}"></i><em>${i===0?'#1':'-'+fmt(100*(max-x[metric])/max,1)+'%'}</em></span><strong class="benchmark-value">${fmt(x[metric])} <small>${label}</small></strong></a>`).join('');document.querySelectorAll('.benchmark-bar i[data-bar-width]').forEach(el=>{el.style.width=`${Math.max(0,Math.min(100,+el.dataset.barWidth))}%`})}
init().catch(e=>document.getElementById('benchmarkRows').innerHTML=`<p class="benchmark-error">${e.message}</p>`);
